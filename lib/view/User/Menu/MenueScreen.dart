import 'package:flutter/material.dart';

class MenueScreen extends StatelessWidget {
  const MenueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: 
       Container(child:const Center(child: Text('MenueScreen'),),));
  }
}